apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: peerprep
spec:
  selector:
    app: matching-service
  ports:
    - protocol: TCP
      port: 3003
      # nodePort: 32657
  type: ClusterIP
