apiVersion: v1
kind: Service
metadata:
  name: client-peerprep
  namespace: peerprep
spec:
  selector:
    app: client-peerprep
  ports:
    - protocol: TCP
      port: 5173
      # nodePort: 32658
  type: ClusterIP
